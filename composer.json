{
    "name": "precision-soft/symfony-doctrine-encrypt",
    "type": "symfony-bundle",
    "description": "Symfony bundle providing transparent AES-256 encryption for Doctrine ORM entity fields via custom DBAL types",
    "keywords": [
        "symfony",
        "doctrine",
        "encrypt",
        "decrypt"
    ],
    "homepage": "https://github.com/precision-soft/symfony-doctrine-encrypt",
    "license": "MIT",
    "authors": [
        {
            "name": "Adrian Jeledintan",
            "email": "adrian.jeledintan@precision-soft.com"
        }
    ],
    "require": {
        "php": ">=8.2",
        "ext-openssl": "*",
        "doctrine/dbal": "4.*",
        "doctrine/orm": "3.*",
        "doctrine/persistence": "3.*",
        "precision-soft/symfony-console": "^4.0",
        "symfony/config": "7.*"
    },
    "require-dev": {
        "friendsofphp/php-cs-fixer": "3.*",
        "phpstan/phpstan": "^2.0",
        "phpstan/phpstan-mockery": "^2.0",
        "phpunit/phpunit": "^11.5",
        "precision-soft/symfony-phpunit": "^3.0"
    },
    "autoload": {
        "psr-4": {
            "PrecisionSoft\\Doctrine\\Encrypt\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "PrecisionSoft\\Doctrine\\Encrypt\\Test\\": "tests/"
        }
    },
    "scripts": {
        "install-hooks": [
            "[ \"${COMPOSER_DEV_MODE:-0}\" -eq 1 ] && { [ -L .git/hooks ] && [ \"$(readlink .git/hooks)\" = ../.dev/git-hooks ] || { rm -rf .git/hooks && ln -s ../.dev/git-hooks .git/hooks; }; } || exit 0"
        ],
        "test": "simple-phpunit --exclude-group integration",
        "test-integration": "simple-phpunit --group integration",
        "phpstan": "phpstan analyse --memory-limit=512M",
        "cs-check": "php-cs-fixer fix --dry-run --diff",
        "cs-fix": "php-cs-fixer fix",
        "deps-audit": "@composer audit --locked",
        "mutation": "infection --threads=max --test-framework-options=\"--exclude-group=integration\" --initial-tests-php-options=\"-d pcov.enabled=1\"",
        "check": [
            "@cs-check",
            "@phpstan",
            "@test"
        ],
        "post-install-cmd": [
            "@install-hooks"
        ],
        "post-update-cmd": [
            "@install-hooks"
        ]
    },
    "minimum-stability": "stable",
    "prefer-stable": true,
    "config": {
        "sort-packages": true
    }
}
